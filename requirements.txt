# API
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
python-multipart>=0.0.9
aiofiles>=23.2.1

# Scraping / extraction
# Sushiscan : bypass Cloudflare via FlareSolverr (conteneur) + curl_cffi (JA3 Chrome).
curl_cffi>=0.7.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
requests>=2.31.0
# DrissionPage : plus requis côté serveur pour Sushiscan, gardé pour l'usage CLI local.
DrissionPage>=4.1.0

# Stockage Telegram (MTProto) — activé seulement si STORAGE_BACKEND=telegram
telethon>=1.36.0
# cryptg : déchiffrement AES en C (au lieu de Python pur) → up/download Telegram
# 2-5x plus rapides. Telethon l'utilise automatiquement. Aucune requête en plus.
cryptg>=0.4.0

# Playlist musique : yt-dlp résout le flux audio YouTube en live (nécessite deno dans
# l'image pour le déchiffrement "n challenge" — voir deploy/Dockerfile).
yt-dlp
# Plugin PO token : yt-dlp récupère le "proof-of-origin token" auprès du sidecar
# bgutil-provider → passe le bot check YouTube de façon fiable sur IP datacenter.
bgutil-ytdlp-pot-provider
# PySocks : support des proxys socks5 (YTDLP_PROXY) pour yt-dlp ET le relais de flux.
PySocks

# Système requis (NON pip) : chromium, xvfb, imagemagick (voir deploy/Dockerfile)
